<% unless @favourite.errors.any? %>
  var itemId = '<%= params[:product_id] %>';
  $('#favourite-remove-' + itemId).addClass('d-none');
  $('#favourite-add-' + itemId).removeClass('d-none');
<% end %>
